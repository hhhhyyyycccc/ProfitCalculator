tests/
# my_package

![license](https://img.shields.io/badge/license-MIT-green.svg)

这是一个最小的 Python 项目骨架（空项目）。

许可证：MIT（见项目根目录的 `LICENSE` 文件）。

快速开始：

1. 进入项目目录：

```bash
cd /Users/heyc/Documents/projectP
```

2. 运行单元测试（使用系统 Python）：

```bash
python3 -m unittest discover -v
```

3. 添加依赖到 `requirements.txt`（如果需要）。

项目结构示例：

```
my_package/
  └── __init__.py
tests/
  └── test_sample.py
README.md
pyproject.toml
requirements.txt
.gitignore
```
